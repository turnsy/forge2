NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Optional: canonical site URL for auth redirects (recommended for custom domains)
# NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Local OAuth (optional — configure in Supabase Dashboard for hosted project)
# SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
# SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
# SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID=
# SUPABASE_AUTH_EXTERNAL_APPLE_SECRET=

# --- Plan generation (v1) ---
# Docs: docs/plan-generation/README.md
# Storage: docs/plan-generation/supabase-session-uploads.md

# Vercel AI Gateway — required for Eve agent turns in local dev (unless using Vercel OIDC).
# Eve agent model defaults to anthropic/claude-sonnet-4 (gateway-routed).
AI_GATEWAY_API_KEY=
# Optional default model for plan-chat (must support tool calling)
# PLAN_CHAT_MODEL=anthropic/claude-sonnet-4
# Optional fast model for chat session titles
# SESSION_TITLE_MODEL=google/gemini-2.5-flash-lite

# Log generated run.py to the Next.js server terminal (on by default in dev)
# PLAN_CHAT_LOG_GENERATED_CODE=1

# Vercel Sandbox — local dev: run `vercel link` in forge-next/, then `vercel env pull .env.local`
# OIDC (preferred on linked projects):
VERCEL_OIDC_TOKEN=
# Or manual token for CI / external machines:
# VERCEL_TOKEN=
# VERCEL_ACCESS_TOKEN=
# VERCEL_TEAM_ID=
# VERCEL_PROJECT_ID=

# Optional: run real sandbox integration tests (Phase 4+)
# RUN_SANDBOX_INTEGRATION=1

/* AUTO-GENERATED — do not edit */

export const FORGE_PLAN_API_CHEAT_SHEET = "forge_plan public API (use these names in submit_plan_code):\n\nPlan.from_json_file(path: 'str') -> \"'Plan'\"\n  Load seed from ``current_plan.json`` (empty template when missing).\n\nPlan.empty(name: 'str') -> \"'Plan'\"\n  Create a new named plan with no weeks.\n\nPlan.is_empty(self) -> 'bool'\n  True when the plan has no weeks (seed not yet built).\n\nPlan.add_week(self, *, label: 'str | None' = None, name: 'str | None' = None, index: 'int | None' = None) -> 'WeekRef'\n  Append a week, or upsert by schema ``index`` when provided.\n\nPlan.add_day(self, *, week_index: 'int', name: 'str | None' = None, index: 'int | None' = None) -> 'DayRef'\n  Append a day to a week, or upsert by schema ``day_index`` when provided.\n\nPlan.add_exercise(self, *, week_index: 'int', day_index: 'int', name: 'str') -> 'ExerciseRef'\n  Append a standalone exercise block to a day.\n\nPlan.add_superset(self, week_index: 'int', day_index: 'int', *, rounds: 'int' = 3, notes: 'str | None' = None) -> 'SupersetRef'\n  Append a superset block with two default exercises.\n\nPlan.add_set(self, week_index: 'int', day_index: 'int', *, reps: 'int | str', load_value: 'float', load_type: 'str' = 'absolute', unit: 'str' = 'kg', notes: 'str | None' = None, exercise_name: 'str | None' = None, exercise_index: 'int | None' = None) -> 'None'\n  Append a planned exact set to a day.\n\nPlan.move_week(self, from_index: 'int', to_index: 'int') -> 'None'\n  Reorder weeks by 0-based array position.\n\nPlan.remove_week(self, index: 'int') -> 'None'\n  Remove a week by 0-based array position.\n\nPlan.move_day(self, week_index: 'int', from_index: 'int', to_index: 'int') -> 'None'\n  Reorder days within a week.\n\nPlan.remove_day(self, week_index: 'int', index: 'int') -> 'None'\n  Remove a day by 0-based array position within a week.\n\nPlan.move_exercise(self, week_index: 'int', day_index: 'int', from_index: 'int', to_index: 'int') -> 'None'\n  Reorder standalone exercise blocks (0-based block positions).\n\nPlan.remove_exercise(self, week_index: 'int', day_index: 'int', index: 'int') -> 'None'\n  Remove a standalone exercise block by 0-based exercise block index.\n\nPlan.move_set(self, week_index: 'int', day_index: 'int', exercise_index: 'int', from_index: 'int', to_index: 'int') -> 'None'\n  Reorder sets within an exercise (0-based flat exercise index).\n\nPlan.remove_set(self, week_index: 'int', day_index: 'int', exercise_index: 'int', index: 'int') -> 'None'\n  Remove a set by 0-based array position.\n\nPlan.write_json(self, path: 'str') -> 'None'\n  Write plan JSON to ``output/plan.json`` (creates parent dirs).\n\nPlan.to_dict(self) -> 'dict[str, Any]'\n  Return JSON-serializable plan data (schema v2.1.0).\n\nSupersetRef (returned by Plan.add_superset):\n\nSupersetRef.add_exercise(self, name: 'str') -> 'ExerciseRef'\n  Append an exercise to this superset with the same round count.\n\n\nSchema validation rules (workout-plan.schema.json v2.1.0 \u2014 sandbox output must pass):\n\nPlan root:\n- schemaVersion must be exactly \"2.1.0\"\n- name: non-empty string (required)\n- weeks: array with at least 1 week for a valid saved plan\n\nWeek:\n- index: integer >= 1 (schema index; forge_plan renumbers to match array order after each mutation)\n- days: at least 1 day per week\n\nDay:\n- index: integer >= 1\n- code: lowercase `w` + week number + `d` + day number (regex: ^w[0-9]+d[0-9]+$)\n  - OK: 'w1d1', 'w2d3', 'w12d4'\n  - Invalid: 'W1D1', 'w1D1', 'W1d1', 'week1-day1', 'w1-d1'\n- blocks: at least 1 block per day (exercise block or superset block)\n- day.code and day.index are assigned by forge_plan from array order (do not pass codes to add_day)\n\nDay block (one of):\n- type \"exercise\": { type: \"exercise\", exercise: Exercise } \u2014 standalone exercise\n- type \"superset\": { type: \"superset\", notes?: string, exercises: Exercise[] } \u2014 grouped exercises\n  - superset must have >= 2 exercises\n  - rounds = number of sets on each exercise (must match across all exercises in the superset)\n  - rest between rounds goes in superset notes (plain text), not a separate field\n\nExercise:\n- name: non-empty string (required)\n- sets: at least 1 set per exercise\n\nSet (forge_plan.add_set builds exact planned sets):\n- id: non-empty string (auto: {{day_code}}-{{exercise_slug}}-{{n}}, e.g. w1d1-bs-1)\n- planned.type: \"exact\" (builder) or \"target\" (hand-authored only)\n- planned.reps: plain integer count (e.g. 5) \u2014 preferred; optional string like 3+1 for rep complexes only\n  - Use a plain integer for set reps (counts only).\n  - Do not put units, sides, ranges, or spreadsheet shorthand in reps (invalid: '3/side', '2x30s', '30s', '5/5', '1 (3 jumps)').\n  - Put qualifiers in planned.notes via add_set(notes=...) \u2014 e.g. notes=\"per side\", notes=\"30 sec\", notes=\"each grip\".\n  - Optional: rep complexes as string 5+5+5 instead of int + notes when splitting reps matters.\n- planned.notes: optional string on exact sets (use for per-side, time, and coaching detail)\n- planned.load: \"absolute\" OR \"percentage\"\n  - absolute: type \"absolute\", value >= 0, unit non-empty string (common: kg, lb, m, yd)\n  - percentage: type \"percentage\", value >= 0, unit non-empty string (lb/kg/etc. \u2014 use add_set unit= for percentage sets)\n- actual: null for new sets\n- status: one of 'planned', 'completed', 'skipped' (builder uses \"planned\")\n- locked: boolean (builder uses false)\n\nforge_plan API conventions:\n- add_week() / add_day(): omit index to append; optional index upserts that slot\n- week_index / day_index on add_*: schema indices (1-based after sync), not 0-based array slots\n- move_* / remove_*: 0-based array positions within the parent list\n- add_exercise: appends a standalone exercise block (type: exercise)\n- add_superset(week_index, day_index, rounds=3, notes=None) -> SupersetRef: appends a superset block with 2 default exercises and N rounds each\n- SupersetRef.add_exercise(name): append another exercise to the superset (inherits round count)\n- For supersets, use add_superset \u2014 do NOT simulate with two separate add_exercise calls\n- add_set: set id/status/locked/actual are automatic; pass notes= for per-side/time/detail; omit exercise_index to target last exercise, or pass exercise_name\n- add_set load_type: only \"absolute\" or \"percentage\" (default absolute)\n- Call add_week \u2192 add_day \u2192 add_exercise or add_superset \u2192 add_set in order for new content\n\nExample run.py (multi-week block \u2014 preferred pattern when user requests a full program):\n\n  from forge_plan import Plan\n\n  WEEKS = 4\n  DAYS_PER_WEEK = 4\n  DAY_NAMES = [\"Lower\", \"Upper\", \"Full body\", \"Accessories\"]\n  LIFT = \"Back Squat\"\n\n  plan = Plan.from_json_file(\"current_plan.json\")\n  if plan.is_empty():\n      plan = Plan.empty(\"4-Week Strength\")\n  for week in range(1, WEEKS + 1):\n      plan.add_week(label=f\"Week {week}\")\n      for day in range(1, DAYS_PER_WEEK + 1):\n          plan.add_day(week_index=week, name=DAY_NAMES[day - 1])\n          plan.add_exercise(week_index=week, day_index=day, name=LIFT)\n          plan.add_set(week_index=week, day_index=day, reps=5, load_value=100, unit=\"kg\")\n  plan.write_json(\"output/plan.json\")\n\nExample run.py (minimal smoke test \u2014 not a full program):\n\n  from forge_plan import Plan\n\n  plan = Plan.from_json_file(\"current_plan.json\")\n  if plan.is_empty():\n      plan = Plan.empty(\"Smoke test\")\n  plan.add_week(label=\"Week 1\")\n  plan.add_day(week_index=1, name=\"Day 1\")\n  plan.add_exercise(week_index=1, day_index=1, name=\"Back Squat\")\n  plan.add_set(week_index=1, day_index=1, reps=5, load_value=100, unit=\"kg\")\n  plan.write_json(\"output/plan.json\")\n\nExample run.py (superset \u2014 use add_superset, not two separate add_exercise calls):\n\n  from forge_plan import Plan\n\n  plan = Plan.from_json_file(\"current_plan.json\")\n  if plan.is_empty():\n      plan = Plan.empty(\"Superset Day\")\n  plan.add_week(label=\"Week 1\")\n  plan.add_day(week_index=1, name=\"Upper\")\n  superset = plan.add_superset(1, 1, rounds=3, notes=\"Rest 90s between rounds\")\n  # Creates a superset block with 2 exercises, 3 sets each (default names Exercise A / B; coach renames in app)\n  superset.add_exercise(\"Face Pull\")  # optional: append a 3rd exercise with matching round count\n  plan.write_json(\"output/plan.json\")\n\nExample run.py (reorder a day):\n\n  from forge_plan import Plan\n\n  plan = Plan.from_json_file(\"current_plan.json\")\n  plan.move_day(week_index=1, from_index=0, to_index=1)\n  plan.write_json(\"output/plan.json\")";

# Phase 7 — Post-create save (stay on `/coach`)

**Status:** Done

## Done criteria

- [x] First save stays on `/coach` with conversation retained
- [x] `planId` set internally after first save via `SET_PLAN_ID`
- [x] Subsequent saves use version API (`useSavePlan(activePlanId)`)
- [x] Back link + unsaved guard work on `/coach` post-save
- [x] `docs/plan-persistence/README.md` decision table updated

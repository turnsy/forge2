# Phase 4 — `set_current_artifact` + edit drop-in

**Status:** Done

## Done criteria

- [x] Agent can load a saved plan into preview via tool call
- [x] LLM never receives full plan JSON from this tool
- [x] `get_plan` / `assign_plan` do not set preview
- [x] Workspace tracks `planId` after load for version saves
- [x] Tests cover tool + client state update
